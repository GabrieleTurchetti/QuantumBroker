{
    "circuit": "OPENQASM 2.0; include \"qelib1.inc\"; gate uf x, y { id x; cx x, y; } qreg q[2]; creg c[2]; x q[1]; h q[0]; h q[1]; uf q[0], q[1]; h q[0]; measure q[0] -> c[0]; measure q[1] -> c[1];",
    "shots": 10000,
    "granularity": 2000,
    "constraints": [
        {
            "target": "machine.technology",
            "value": "simulator",
            "op": "eq"
        }
    ],
    "policies": [
        "green-50",
        "reliable-50"
    ]
}